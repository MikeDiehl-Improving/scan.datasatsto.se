Preserve the random scan lookup endpoint (`randomScan` handler in server.js, routes `GET /random/:secret` and `GET /random/:secret/:code`): it executes the `Scan.Get_Random` stored procedure with the given event secret and optional reference/vendor code, and returns whatever recordset the database yields as a JSON array with HTTP 200. There is no count/batch parameter — the second URL segment (when present) is a reference code filter, not a quantity.

Important existing quirk to preserve exactly: `randomScan`'s callback (`async function(recordset) { res.status(200).json(recordset); }`) does NOT check whether `recordset` is defined, unlike other handlers in this file. Tracing `sqlQuery`: when the SQL statement fails, `statementComplete` sets `errMsg` and invokes the completion callback (`next()`) with NO arguments, and `requestCompleted` then skips its own `next(rows)` call because `errMsg` is set. So on a database/statement error, `randomScan`'s callback still runs exactly once, with `recordset === undefined`, and it calls `res.status(200).json(undefined)` — Express serializes that to an HTTP 200 response with an EMPTY body (no HTML error page, no 500). There is no try/catch path that produces a 500 here because the error surfaces asynchronously via the event-driven SQL callback, not a thrown exception. Do not assert a 500/HTML-error-page outcome for this endpoint's failure case — assert the actual HTTP 200 + empty-body behavior. Cover both the with-code and without-code route variants using the existing HTTP-level test harness (mocking/stubbing the SQL query call), matching current behavior exactly — do not invent count-based semantics or a 500 error path that doesn't exist.
